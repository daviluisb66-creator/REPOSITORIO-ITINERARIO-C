#include <iostream>
using namespace std;

int main() {
    int x, y, soma;

    cout << "Digite o primeiro valor: ";
    cin >> x;
    cout << "Digite o segundo valor: ";
    cin >> y;

    soma = x + y;

    cout << "Soma = " << soma << endl;

    return 0;
}
